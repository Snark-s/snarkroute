# Example HTTP JSON Request

This is a declarative SnarkRoute node package. It has no custom executor code.

It uses `executor.type: "declarative.http"` to call `http://127.0.0.1:4317/api/health` and map the JSON response into declared outputs.
