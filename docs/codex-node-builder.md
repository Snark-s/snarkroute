# Codex Node Builder

Codex should use:

```text
.codex/skills/snarkroute-node-builder/SKILL.md
```

The skill explains how to create development folder packages, plugin executors, declarative HTTP nodes, README files, example routes, library manifests, and portable `.snarknode` archives.
