import { describe, expect, it } from "vitest";
import { gatewayParameters, nearestAspectRatio, parameterValidationErrors, parametersFromComposition } from "./parameters";
describe("gatewayParameters", () => { it("drops unknown and invalid values while preserving schema ids", () => { expect(gatewayParameters([{ id: "duration", type: "number" }, { id: "enabled", type: "boolean" }, { id: "resolution", type: "select", options: [{ value: "720p" }] }], { duration: "5", enabled: true, resolution: "4k", secret: "no" })).toEqual({ duration: 5, enabled: true }); }); });
describe("schema validation and composition size", () => { it("blocks missing required fields", () => { expect(parameterValidationErrors([{ id: "width", label: "Width", type: "number", required: true }], {})).toEqual(["Width is required."]); }); it("rounds composition dimensions to schema constraints", () => { expect(parametersFromComposition([{ id: "width", type: "number", min: 256, max: 2048, multipleOf: 64 }, { id: "height", type: "number", min: 256, max: 2048, multipleOf: 64 }], {}, 1919, 1079).values).toEqual({ width: 1920, height: 1088 }); }); it("selects the closest supported aspect ratio", () => { expect(nearestAspectRatio(1920 / 1080, ["1:1", "4:3", "16:9"])).toBe("16:9"); }); });
