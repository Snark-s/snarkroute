# Example Caddy config for the Docker deployment.
# Copy to Caddyfile or set PUBLIC_APP_URL in the deployment environment.

{$PUBLIC_APP_URL} {
  encode zstd gzip

  handle /api/* {
    reverse_proxy api:4317
  }

  handle {
    reverse_proxy web:80
  }
}
