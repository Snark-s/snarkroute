{
  "name": "@snarkroute/model-catalog",
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc src/index.ts --declaration --declarationMap --outDir dist --module NodeNext --moduleResolution NodeNext --target ES2022 --strict --skipLibCheck",
    "test": "vitest run"
  }
}
